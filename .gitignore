node_modules/
dist/
bin/
out/
*.vsix
.DS_Store

# Keep .vscode/ tracked (shared settings) but not user-local stuff
.vscode/*.log

.env

# Loom per-workspace local state (scratchpad, index, workspace skills/agents/commands)
.loom/
