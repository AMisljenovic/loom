---
name: <tool_name>
category: read | write | execute | subtasks
requires_approval: true | false
---

## Purpose
One sentence on what the tool does. Loaded into the tool catalogue as the
tool's `Description` (the one-liner the model sees in the tool list).

## When to use
- Bullet
- Bullet

## When NOT to use
- Bullet
- Bullet

## Input
Describe each input field beyond what JSON Schema captures. Why does the model
care about each one?

## Behavior
Quirks the model needs to know. Edge cases, side effects, post-call host
behavior.

## Examples

```json
{"field": "value"}
```

Notes on each example.
